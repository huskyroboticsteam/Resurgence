#include "Graph.h"
#include<vector>

// void Graph::updateRobotLocation(Node newPoint)
// {
//     robot_current_location = newPoint;
// }

// void Graph::createNode(Point p)
// {
//     map.push_back(Node{p, false});
// }

// double Graph::distanceBetweenNodes(node_ptr a, node_ptr b)
// {
//     return distance(a->location, b->location);
// }

// Node Graph::getNode(Point p)
// {
//     int i = 0;
//     while(i < map.size())
//     {
//         if(map.at(i).location.equals(p)){
//             return map.at(i);
//         }
//         i++;
//     }
// }

Graph::Graph()
{
    
    // for(int i = -1 * Graph::radius; i <= Graph::radius; i += Graph::step)
    // {
    //     for(int j = -1 * Graph::radius; j <= Graph::radius; j += Graph::step)
    //     {
    //         createNode(Point{(double)i, (double)j});
    //     }
    // }
    // robot_current_location = getNode(Point{0,0});
}
#pragma once

namespace cam {
// dummy CameraParams class, so the type isn't incomplete anymore
class CameraParams {};
} // namespace cam

namespace cv {
// same as above
class Mat {};
} // namespace cv

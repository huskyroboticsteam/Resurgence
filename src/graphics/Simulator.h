#pragma once

class Simulator
{
    // take in a vector of points (large dots?) and a map obstacle (large dots different color?)
    //  and graph them
    // also graph robot (rect) location and predicted direction (line)       
   
    // run robot pathfinding algorithm and move robot

    //we need to preprogram maps of obstacles
    //need to make robot move according to pather


    
}
name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: mkdirbuild
      run: mkdir build
    - name: cmake
      run: cmake --build build
    - name: test
      run: ./build/tests
